import 'package:flutter_test/flutter_test.dart';

void main() {
  // testWidgets('AppBar in mobile device', (tester) async {
  //   await mockNetworkImages(() async {
  //     await tester.pumpApp(Builder(builder: (context) {
  //       return Scaffold(
  //         appBar: AppBarSection(context: context, isMobileView: true),
  //       );
  //     }));
  //   });

  //   // Actual
  //   final appBar = find.byType(AppBarSection);

  //   // Assert
  //   expect(appBar, isA<PreferredSizeWidget>());

  //   // Check Image Logo
  //   expect(find.byType(Image), findsOneWidget);

  //   // Check component only in desktop/tablet mode
  //   expect(find.byType(TextFormField), findsNothing);
  // });

  // testWidgets('AppBar in tablet/desktop device', (tester) async {
  //   await mockNetworkImages(() async {
  //     await tester.pumpApp(Builder(builder: (context) {
  //       return Scaffold(
  //         appBar: AppBarSection(context: context, isMobileView: false),
  //       );
  //     }));
  //   });

  //   // Actual
  //   final appBar = find.byType(AppBarSection);

  //   // Assert
  //   expect(appBar, isA<AppBar>());

  //   // Check Image Logo
  //   expect(find.byKey(const Key('img_logo_appbar')), findsOneWidget);
  //   expect(find.byType(TextFormField), findsOneWidget);
  // });

  // testWidgets('AppBar actions', (tester) async {
  //   await mockNetworkImages(() async {
  //     await tester.pumpApp(Builder(builder: (context) {
  //       return Scaffold(
  //         appBar: AppBarSection(context: context),
  //       );
  //     }));
  //   });

  //   // Assert
  //   expect(find.byType(IconButton), findsNWidgets(3));
  // });
  //
  test('TODO: testing in AppBarSection', () {
    expect(true, true);
  });
}
